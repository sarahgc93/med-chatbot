# Medical Chatbot

Creating a chatbot for medical questions. Note that this repo requires an .env file. See .env sample and set yours up accordingly.

To build:
- `make build`


- environment for notebook
    - `python3.12 -m venv venv`
    - `source venv/bin/activate`
    - `pip install -r requirements.txt`


